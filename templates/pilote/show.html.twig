{% extends 'base.html.twig' %}

{% block title %}Pilote{% endblock %}

{% block body %}
    <h1>Pilote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pilote.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pilote.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ pilote.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ pilote.age }}</td>
            </tr>
            <tr>
                <th>Ecurie</th>
                <td>{{ pilote.ecurie }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ pilote.numero }}</td>
            </tr>
            <tr>
                <th>NombreCourse</th>
                <td>{{ pilote.nombreCourse }}</td>
            </tr>
            <tr>
                <th>NombrePodium</th>
                <td>{{ pilote.nombrePodium }}</td>
            </tr>
            <tr>
                <th>Victoire</th>
                <td>{{ pilote.victoire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pilote_index') }}">back to list</a>

    <a href="{{ path('pilote_edit', {'id': pilote.id}) }}">edit</a>

    {{ include('pilote/_delete_form.html.twig') }}
{% endblock %}
